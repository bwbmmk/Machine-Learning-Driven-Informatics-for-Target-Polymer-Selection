# E02 试用一个读取分子字符串的工具（选做）

有些分子可以用一串文字表示。你要练习调用 RDKit，让它尝试读懂字符串，再把计算结果整理成字典。需要一点新背景，可最后再做。

先完成：P07；需 rdkit，选做。本关主要练：安装第三方库、调用工具、判断解析失败、组织返回结果。

第一次做题，先读 [怎么开始](../../START_HERE.md)。下面所有命令都在有 `check.py` 的 `python_handson_pack/` 目录运行。

## 要做成什么样

`describe_smiles("CCO")` 应表示读取成功。按 RDKit 默认分子图，它有 3 个显式原子、0 个环；分子量由工具计算。
输入空字符串或明显错误的字符串，应返回 valid=False，其余三项为 None。不能用字符串长度冒充分子原子数。

## 打开哪里，按什么顺序写

先打开 `lessons/e02_rdkit/task.py`。下面每一步都可以单独当作一次小练习；不要求一次写完整关。

1. **先装好 RDKit**：按 ENVIRONMENT.md 准备环境，先单独试着读取 CCO；不要求先学习整门化学。
2. **先处理失败**：在 describe_smiles 中检查输入是不是非空文字，解析失败时按规定返回结果。
3. **再处理成功**：用工具提供的方法计算原子数、分子量、环数，填入普通字典。
4. **多试几条**：读 data/smiles_examples.json，逐条调用函数，把结果存到自己新建的 output 目录。

## 先检查一小步

完全选做。前面的 Python 文件处理和数据练习不依赖这一关。

都写完后，再运行整关检查：

```powershell
py check.py E02
```

如果电脑使用 `python`，把命令里的 `py` 换成 `python`。这条命令会运行一组检查；只完成一小步时，其他检查失败是正常的。先看报错最后几行，只解决当前这个问题。

普通关卡不要改 `test_task.py`；它是已经准备好的检查程序。P06 的 `test_student.py` 则明确留给你填写。只定义函数后直接运行 task.py 可能没有输出，这不等于写错；使用本关检查命令或自己写调用。

## 遇到这些词怎么理解

SMILES 是一种表示分子结构的文字写法；“解析”是让工具读懂它；“描述符”是用一个数字描述某个特点。

## 写完后核对的具体规则

这部分保留准确的函数名、返回格式和特殊情况，方便检查，也方便其他 AI 辅导。第一次不用全背下来；写到哪个函数，再对照哪条。

describe_smiles(text) -> {'valid':bool,'num_atoms':int或None,'mol_wt':float或None,'num_rings':int或None}。
非字符串、空白、解析失败均返回 valid=False，其余三项 None；合法时用 RDKit 的分子图和描述符计算，不用字符计数替代原子数。
只处理提供的简单分子示例。含 * 的聚合物重复单元规范和科学意义不在本关验收范围；继续做真实项目时需要另行核查。

## 完成以后再做

**基本完成**：函数按上述规则通过自动检查；需要展示或文件输出的部分，你也亲自看过结果。自动检查通过不代表后面的加练已经完成。

**再试着解释**：说明 MolWt 在这里是有限分子的描述符，不能直接当成整条聚合物链的分子量。

**加练，主任务完成后再做**：画出两种结构，并用程序比较它们的原子数和环数；不解释成 Tg 因果规律。

加练写在 `variations.py`，自己补一个能核对结果的例子；原来的自动检查不一定检查加练。最后在 `NOTES.md` 写三句话：我写了什么、实际运行结果、哪里还不懂。

卡住时可以这样问 AI：“我在 E02 的第 __ 步，请先用日常语言解释这一步，再给一个不同输入的小例子。不要一次给我整关答案。”
