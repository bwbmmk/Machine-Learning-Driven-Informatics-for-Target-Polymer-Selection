# P09 按温度筛选、排名，再算平均值

整理好资料后，你想知道哪些在指定温度范围内、哪些温度最高、总体平均是多少。这和按成绩筛选和排名很像。

先完成：P05；建议完成 P08。本关主要练：条件判断、sorted、平均数、空列表处理。

第一次做题，先读 [怎么开始](../../START_HERE.md)。下面所有命令都在有 `check.py` 的 `python_handson_pack/` 目录运行。

## 要做成什么样

假设四条记录的 tg_k 依次是 300、350、350、400。
筛选 300 到 350 应保留前三条，两个端点都包括。
取最高的两条，应先取 400，再取输入中先出现的 350。
全部四条的 count=4、min=300、max=400、mean=350。

## 打开哪里，按什么顺序写

先打开 `lessons/p09_stats/task.py`。下面每一步都可以单独当作一次小练习；不要求一次写完整关。

1. **写范围筛选**：filter_range 逐条检查 low<=tg_k<=high；low 比 high 大时，报 ValueError。
2. **写前几名**：top_n 按温度从高到低排序，返回前 n 条；相同温度维持原来先后。不要把原列表原地排乱。
3. **写统计**：summarize 对传进来的整份记录统计；空列表时 count=0，其余三项都是 None。
4. **用真实教学输入连接**：从 P01 读取，再经 P05 清洗，将 cleaned 交给本关函数。这里“真实输入”指文件里的教学资料，不是实验数据。

## 先检查一小步

这三个函数接收的是已经有 tg_k 的清洗结果；不要直接把尚未清洗的原 JSON 交给它们。

都写完后，再运行整关检查：

```powershell
py check.py P09
```

如果电脑使用 `python`，把命令里的 `py` 换成 `python`。这条命令会运行一组检查；只完成一小步时，其他检查失败是正常的。先看报错最后几行，只解决当前这个问题。

普通关卡不要改 `test_task.py`；它是已经准备好的检查程序。P06 的 `test_student.py` 则明确留给你填写。只定义函数后直接运行 task.py 可能没有输出，这不等于写错；使用本关检查命令或自己写调用。

## 遇到这些词怎么理解

“闭区间”就是包含两端；“稳定排序”就是相同数值仍保持原先后；“摘要”在这里就是四个统计数字。

## 写完后核对的具体规则

这部分保留准确的函数名、返回格式和特殊情况，方便检查，也方便其他 AI 辅导。第一次不用全背下来；写到哪个函数，再对照哪条。

filter_range(records, low, high) -> 列表，包含 low<=tg_k<=high；low>high 抛 ValueError。
top_n(records,n) -> 最高温前 n 条；n 为整数，负数抛 ValueError，0 返回 []。
summarize(records) -> {'count':int,'min':float或None,'max':float或None,'mean':float或None}。
三个函数不改变输入。已清洗数据的 tg_k 假定为有限数值。
6 条清洗数据中，300–350 K 筛得 EX02、EX10、EX11；此为规则筛选，不是预测。

## 完成以后再做

**基本完成**：函数按上述规则通过自动检查；需要展示或文件输出的部分，你也亲自看过结果。自动检查通过不代表后面的加练已经完成。

**再试着解释**：拿纸算 [300,350,350,400] 的结果，解释为什么 count 和筛选后 count 不是同一统计口径。

**加练，主任务完成后再做**：新增 median 字段并写手算检查；不要改原 summarize 的接口，另写 summarize_extended。

加练写在 `variations.py`，自己补一个能核对结果的例子；原来的自动检查不一定检查加练。最后在 `NOTES.md` 写三句话：我写了什么、实际运行结果、哪里还不懂。

卡住时可以这样问 AI：“我在 P09 的第 __ 步，请先用日常语言解释这一步，再给一个不同输入的小例子。不要一次给我整关答案。”
