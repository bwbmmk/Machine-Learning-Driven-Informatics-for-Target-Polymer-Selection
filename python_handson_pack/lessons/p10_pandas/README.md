# P10 用 Pandas 再做一次筛选和统计

Pandas 是操作表格的 Python 工具。把上一关的工作用它再做一次，比较两种写法，理解它能帮你省掉哪些循环。

先完成：P09；需 pandas。本关主要练：安装库、DataFrame、选行、排序、表格统计。

第一次做题，先读 [怎么开始](../../START_HERE.md)。下面所有命令都在有 `check.py` 的 `python_handson_pack/` 目录运行。

## 要做成什么样

同一份清洗记录，用 P09 和本关处理，筛选出的编号、排名编号和统计结果应该一致。
例如空输入应交出：

```python
{"filtered_ids": [], "top_ids": [],
 "stats": {"count": 0, "min": None, "max": None, "mean": None}}
```

## 打开哪里，按什么顺序写

先打开 `lessons/p10_pandas/task.py`。下面每一步都可以单独当作一次小练习；不要求一次写完整关。

1. **到这关再安装**：按 ENVIRONMENT.md 安装 pandas。确保运行检查的 Python 正是安装库的那个。
2. **先变成表格**：在 dataframe_report 中把记录列表交给 DataFrame，观察列名，把 tg_k 转为数值。
3. **逐项做出结果**：先做范围筛选，再做前 n 名，最后算全表统计。把编号列表和统计字典装进上面的结果格式。
4. **对照旧实现**：同样输入调用 P09，核对结果。再补空输入、坏数字、low>high、n<0 的处理。

## 先检查一小步

本题应实际用 Pandas 做筛选和统计；P09 用于对照答案，不是替代本题全部实现。

都写完后，再运行整关检查：

```powershell
py check.py P10
```

如果电脑使用 `python`，把命令里的 `py` 换成 `python`。这条命令会运行一组检查；只完成一小步时，其他检查失败是正常的。先看报错最后几行，只解决当前这个问题。

普通关卡不要改 `test_task.py`；它是已经准备好的检查程序。P06 的 `test_student.py` 则明确留给你填写。只定义函数后直接运行 task.py 可能没有输出，这不等于写错；使用本关检查命令或自己写调用。

## 遇到这些词怎么理解

“库”是别人写好的工具集合；DataFrame 可理解成带行和列的表格；“聚合”在这里指计算总数、最小值或平均值等。

## 写完后核对的具体规则

这部分保留准确的函数名、返回格式和特殊情况，方便检查，也方便其他 AI 辅导。第一次不用全背下来；写到哪个函数，再对照哪条。

dataframe_report(records, low, high, n) -> {'filtered_ids':list,'top_ids':list,'stats':P09摘要}。
filtered 保留原顺序，top 稳定降序；统计针对全表，非筛选结果。支持 CSV 常见的数值字符串；空输入给空 ID 列表和空摘要。
low>high 或 n<0 抛 ValueError；不可解析/非有限 tg_k 抛 ValueError；不静默删行。
核心筛选和聚合应使用 Pandas，不能只转调 P09 以通过检查。

## 完成以后再做

**基本完成**：函数按上述规则通过自动检查；需要展示或文件输出的部分，你也亲自看过结果。自动检查通过不代表后面的加练已经完成。

**再试着解释**：指出标准库版和 Pandas 版各自适合什么场景；自动验收不检查你是否实际用了 Pandas，需要自行/请 AI 审阅。

**加练，主任务完成后再做**：从你导出的 CSV 读入，加入空白单元格，说明错误处理与 fillna(0) 的差别。

加练写在 `variations.py`，自己补一个能核对结果的例子；原来的自动检查不一定检查加练。最后在 `NOTES.md` 写三句话：我写了什么、实际运行结果、哪里还不懂。

卡住时可以这样问 AI：“我在 P10 的第 __ 步，请先用日常语言解释这一步，再给一个不同输入的小例子。不要一次给我整关答案。”
