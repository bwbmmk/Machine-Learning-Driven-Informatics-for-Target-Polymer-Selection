# P11 把温度画成两张图片

数字多了不容易看。你要生成两张图：一张看温度主要集中在哪些范围，一张比较每条记录的温度。

先完成：P09；需 matplotlib，P10建议完成。本关主要练：Matplotlib、图标题、横纵轴、保存 PNG、关闭图。

第一次做题，先读 [怎么开始](../../START_HERE.md)。下面所有命令都在有 `check.py` 的 `python_handson_pack/` 目录运行。

## 要做成什么样

完成后输出目录应有：

- histogram.png：按温度范围统计条数的直方图。
- by_id.png：每个编号对应一根柱子的柱状图。

这里没有唯一标准的画面样式，但应能读清编号、温度单位和“这是教学数据”的说明。

## 打开哪里，按什么顺序写

先打开 `lessons/p11_plot/task.py`。下面每一步都可以单独当作一次小练习；不要求一次写完整关。

1. **准备工具和数据**：按 ENVIRONMENT.md 安装 matplotlib；输入使用 P05 的 cleaned。
2. **先画一张柱状图**：横轴是编号，纵轴是 tg_k，保存成 by_id.png。先打开图片看字是否清楚。
3. **再加直方图**：把温度列表按区间计数，保存 histogram.png。图上注明 synthetic teaching data 和 Tg (K)。
4. **补函数规则**：空列表直接返回 []，不创建目录。写图前检查两个文件都不存在。保存并关闭图，最后按 histogram、by_id 顺序返回两个 Path。
5. **写一小段观察**：在 NOTES.md 中写 100–200 字：你从图上看到了什么、为什么这些教学数据不能代表真实材料规律。

## 先检查一小步

make_plots 负责保存图片，不要求弹出窗口。生成之后再用文件管理器打开 PNG 看结果。

都写完后，再运行整关检查：

```powershell
py check.py P11
```

如果电脑使用 `python`，把命令里的 `py` 换成 `python`。这条命令会运行一组检查；只完成一小步时，其他检查失败是正常的。先看报错最后几行，只解决当前这个问题。

普通关卡不要改 `test_task.py`；它是已经准备好的检查程序。P06 的 `test_student.py` 则明确留给你填写。只定义函数后直接运行 task.py 可能没有输出，这不等于写错；使用本关检查命令或自己写调用。

## 遇到这些词怎么理解

“直方图”是按数值范围计数；“后端”是绘图库实际生成图的方式，此题可以选 Agg 来直接写图片。

## 写完后核对的具体规则

这部分保留准确的函数名、返回格式和特殊情况，方便检查，也方便其他 AI 辅导。第一次不用全背下来；写到哪个函数，再对照哪条。

make_plots(records, output_dir) -> 两个 Path 的列表，顺序为 histogram.png、by_id.png；空输入返回 [] 且不创建输出目录。
输入为清洗后有限数值。非空时创建输出目录；任一目标已存在时抛 FileExistsError，检查发生在绘图前。
函数使用无界面后端或可在无界面环境运行，不调用阻塞的 show。图标题、刻度可读性需人工检查。

## 完成以后再做

**基本完成**：函数按上述规则通过自动检查；需要展示或文件输出的部分，你也亲自看过结果。自动检查通过不代表后面的加练已经完成。

**再试着解释**：在 NOTES 中记录实际打开过的图，指出任意一个可能误导读者的表达并修正。

**加练，主任务完成后再做**：改两种直方图分箱数并比较；补一段说明为什么 6 条教学记录不支持总体规律判断。

加练写在 `variations.py`，自己补一个能核对结果的例子；原来的自动检查不一定检查加练。最后在 `NOTES.md` 写三句话：我写了什么、实际运行结果、哪里还不懂。

卡住时可以这样问 AI：“我在 P11 的第 __ 步，请先用日常语言解释这一步，再给一个不同输入的小例子。不要一次给我整关答案。”
