# 材料生成工具

`build_pack.py` 是初始练习包生成脚本，日常做题不需要运行。

它会直接写入 `python_handson_pack/` 中的题目、代码骨架和文档；导入这个脚本也会执行顶层生成逻辑。已有手写练习后不要运行或导入它，以免覆盖自己的实现。

本次仅适配整理后的源数据和 ZIP 路径，未重新生成练习包。脚本输出 ZIP 到 `releases/`；生成后仍需另行检查材料并更新校验文件。
